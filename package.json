{
  "name": "carpe-diction",
  "version": "1.0.0",
  "description": "CarpeDiction.com - A dictionary-like compendium with lexical APIs",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd client && yarn install && yarn build",
    "postinstall": "cd client && yarn install",
    "client:start": "cd client && yarn start",
    "client:build": "cd client && yarn build",
    "client:test": "cd client && yarn test",
    "client:eject": "cd client && yarn eject",
    "install:all": "yarn install && cd client && yarn install"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "bcrypt": "^5.1.1",
    "cheerio": ">=1.0.0 <1.1.0",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^7.8.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  },
  "engines": {
    "yarn": ">=1.0.0",
    "node": ">=14.0.0"
  },
  "resolutions": {
    "cheerio": "1.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://git.heroku.com/carpe-diction.git"
  }
}
